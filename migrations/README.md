# Migraciones — qué agregó cada una

Historial movido desde `CLAUDE.md` § Stack el 16-sep-2026, byte por byte. Las migraciones son solo aditivas (tablas nuevas y columnas nullable); el orden lo fija el número. Una migración puede además **declarar** una columna que ya existía por `alter table` de tiempo de ejecución: es aditiva por definición (`if not exists`) y hace que el esquema se pueda leer en un solo lugar. `npm run db:migrate` (`scripts/migrate.mjs`) las aplica en cada deploy, cada una en su propia transacción.

- Schema: `migrations/*.sql` (0014 = bitácora, archivos, corte idempotente; 0016 = `products.ref_cost`; 0017 = dos márgenes/precios por partida, `accepted_offer`, plazo de la solicitud; 0018 = copia marcada del margen viejo y muerte del 12% por omisión; 0019 = sin valores por omisión de negocio: `company_settings` sin defaults ni NOT NULL, `fega_commission`, `early_pay_days`, sin `default_tiie`, `partners.payment_days` sin default, el 12% de migración pasa a "sin margen"; 0020 = escalera de plazos `company_settings.quote_terms`, sembrada 0/30/60/90/120/150; 0021 = `credit_policies.charge_commission` / `charge_fega`, nacen sin capturar y no se tocan las políticas que ya existían; 0022 = la decisión del dueño capturada: GRUPO_SL sí/sí, ESTANDAR no/no, NONE no/no, sin pisar lo que ya se contestó a mano; 0023 = `invoices.calc_client`, la explicación de la FI para el cliente; 0024 = catálogo `credit_circuits` (comisión ASR copiada de Ajustes) y tabla de tasas `funding_rates` de dos columnas, vacía; 0025 = **etiqueta** `circuit_code` en solicitudes/cotizaciones/pedidos/facturas, nula permitida, lo existente asignado por plazo — 0 → CONTADO, lo demás y el corte → ASR, FI/ATC/NC heredan de su origen, `route_kind` no cuenta; 0026 = **el motor lee el circuito**: `quotes.commission_rate` / `cost_rate` / `collection_rate` congeladas (lo existente con la comisión del Circuito ASR del catálogo — idéntico al centavo), `quote_lines.disbursed_unit`, Santa Rosa comisión 0 escrita, `company_settings.asr_commission` TIRADA — la comisión vive solo en `credit_circuits`; 0027 = puente con Compaq: `invoices.folio_fiscal` / `uuid_fiscal` / `supplier_folio`, texto libre, nacen vacías, capturadas después de emitido el documento; 0028 = `company_settings.quote_validity_days`, la vigencia de la cotización sale de Ajustes, no del código; 0029 = **BLOQUE DE DESHACER, paso 0**: `cancelled_at`/`cancelled_by`/`cancel_reason` en solicitud/cotización/pedido/OC/factura, `reverses_id` en factura/pago/banco/inventario — cimientos, nada se cancela ni se revierte todavía; 0030 = `invoices.sat_cancelled_at`, cuándo se canceló ante el SAT un CFDI que el sistema revirtió, nula = pendiente; 0031 = `member_favorites`, la estrella del menú por persona (`member_id` + `section_key` de `nav.ts`), nace vacía, sin favoritos por omisión; 0032 = `sales_lines.qty_returned` en migración de verdad; 0033 = `folio_counters`, contador por serie de kardex (A4); 0034 = cimientos del borrado de datos de prueba y `company_settings.live_since`; 0035 = **`event_ref`** (texto, nulo permitido) en `stock_moves` e `invoices`: a **cuál** recepción o entrega pertenece cada movimiento y cada factura — `origin` sigue guardando el folio de la OC/pedido, compartido por todas; 0036 = `invoices.applies_to_id` (entero, nulo permitido): a **cuál** factura abonó una NC de devolución (Decisión 52, paso 5 de parciales), sin llave foránea como `reverses_id`; 0037 = **cierre corto** (paso 7 de parciales, Decisiones 49-50): `qty_closed_short` en `purchase_lines` y `sales_lines` (numérica, 0) — la cantidad que ya nunca va a llegar/salir — y el trío `closed_short_at` / `closed_short_by` / `closed_short_reason` en `purchase_orders` y `sales_orders`, como el de cancelar); 0038 = **bancos del corte** (Decisión 63): `bank_moves.cutover_key` (texto, nulo permitido) e índice único parcial — el saldo inicial de cada cuenta entra por `/importar` como movimiento de banco con fecha, idempotente, en vez de pisar `banks.opening`; 0039 = **plazo vacío** (Decisión 67): `partners.payment_days` deja de ser `not null` — quita una restricción, no toca datos ni tipo; el catálogo y la ficha siembran a los socios nuevos SIN plazo (null), los existentes se quedan como están; 0040 = **nacimiento del crédito** (Decisiones 68-69): `policy_code` de `sales_orders` e `invoices` sin `default 'NONE'` y sin `not null` — la política se elige o el documento no nace; vacío = sin política capturada; lo que ya está en NONE no se toca — y `credit_days` de los pedidos a Fecha/Cosecha (y sus FV de producto) que nacieron con 0 se deriva de sus propias fechas (`credit_due − date`), nunca de Ajustes; 0041 = **folios de dinero por serie** (grupo C de la auditoría): siembra en `folio_counters` el máximo real de FV/FP/NC/FI/ATC (de `invoices`) y PAG (de `payments`) por empresa — sin tablas ni columnas nuevas, nada se renumera; desde aquí esos folios salen de `nextDocFolio`, no de `count(*)`; 0042 = **devolución por partida** (grupo D de la auditoría): `invoice_lines.line_id` nullable → `sales_lines(id)`, de qué partida sale cada renglón de la NC de una devolución; las NC anteriores quedan en null y su reversa sigue por producto, como se escribieron; 0043 = **política de cobro en la ficha del cliente** (Decisión 73): `partners.policy_code` nullable; siembra única GRUPO_SL a los socios con `group_name = 'Grupo SL'` si la política existe en su empresa; nadie más recibe política 0044 = moneda del costo (Decisiones 76-81): `cost_currency`/`cost_fx` en `quote_lines` y `customer_request_lines`, `partner_products.currency`, `vendor_rfqs.currency` (antes por código), `products.ref_cost_currency`/`ref_cost_fx`, `bank_moves.fx_rate`/`amount_fx`/`pair_id`, y backfill de FP USD sin `cutover_key` al molde FV (`amount` en pesos, `amount_fx` original). 0045 = **el esquema se declara en la migración, no en el código** (18-sep-2026): declara cuarenta columnas, veinticuatro de las cuales existían únicamente porque algún camino corría `alter table ... add column if not exists` antes de usarlas (las otras dieciséis, los márgenes por partida, ya venían de 0017/0018 y se repiten para leer el grupo completo en un lugar) — entre ellas `purchase_orders.so_id` (la única liga entre una compra y el pedido que la motivó) y `purchase_orders.fulfill_kind` (inventario vs. directo/brokeraje). Todas quedan declaradas, con `if not exists`, así que en una base donde el código ya las creó no cambia nada. Agrega dos índices que no existían y sí se caminan en bucle: `po_so_idx (company_id, so_id)` e `inv_origin_idx (company_id, kind, origin)`, el camino pedido → compra → factura del proveedor.

**0046 — `payment_allocs.applied_at`** (20-sep-2026, L3c). La aplicación de un cobro a una factura lleva su propia fecha. Hasta hoy heredaba la del cobro; con el saldo a favor (L5) un cobro se aplica meses después de nacer, y la factura quedó pagada el día de la APLICACIÓN. Nullable: vacío = el mismo día que el cobro (lo que siempre fue); todo lector usa `coalesce(pa.applied_at, p.date)`, así que ningún número histórico se mueve. Resuelve el rodeo por memo de la rama `l3c-en-pausa` (que hacía retroceder la fecha de pago a un abono viejo), el saldo a favor a una fecha de corte, y que desaplicar moviera un mes cerrado.
